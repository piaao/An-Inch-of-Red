# room_scene 游戏化方案书

> 两个可玩方案：**「伪物」（捉迷藏）** 与 **「寻红」（找红包）**。
> 本文所有数字都从现有工程里算出来或测出来 —— `js/layout.js` 算归属，GLB 算颜色，
> `data/kit_three.json` 算尺寸，`scripts/derive_gamedata.py` 一键复现。
> 没有一处是估的。

---

## 0. 结论先行

| | 方案一 ·「伪物」捉迷藏 | 方案二 ·「寻红」找红包 |
|---|---|---|
| 一句话 | 你是会走路、会跳的家具；混不进去就得自己搬过去 | 小人视角进屋，限时找齐所有红包 |
| 类型 | **非对称对抗（1 搜 N 藏）+ 逻辑推理** | **单人视觉搜索**（可升级为潜行） |
| 核心创新 | **归属律**：道具的"合法房间"是公开规则，搜捕者能做排除法，躲藏者能拿到赌注 | ~~**近色博弈**~~ **⚠️ 实测否掉**：ΔE 19.6 成立，但红家具是**路标不是障碍**（§3.8） |
| 可玩点 | 表演（静止本身有压力）+ 双向猜心 | 顿悟（"我从它旁边走过"）+ 扫视路径优化 |
| 动力 | 4× 分值的"远客"卡牌迫使玩家贪一次；换边；被识别时刻复盘 | 每日种子 + 星级 + 连击 + 排行榜 |
| 主要难点 | tell 透明度的调校（**成败单点**） | 没有对手 ⇒ 无戏剧张力 |
| 技术复用度 | **是方案二的超集** | 方案一做完后是减法 |
| 落地成本 | 中（角色控制器 + AI 需新写，其余全现成） | 中低（但需新建红包模型） |
| 我的建议 | ✅ **先做这个** | 作为方案一的**教学关 / 单人模式** |

---

## 1. 前提：一个必须先在数据上解决的事 —— 尺度

这一节不是铺垫，是**两个方案共同的地基**。不先解决它，两个玩法都会当场崩掉。

### 1.1 事实

| 量 | 值 | 来源 |
|---|---|---|
| 外墙高度 `WALL_H` | **1.290** | `js/layout.js` |
| 地板砖 `floorFull` | **1.000 × 0.050 × 1.000** | `data/kit_three.json` |
| 平面 `PLAN` | 10 × 8（80 块砖） | `js/layout.js` |
| 双人床 `bedDouble` | 0.956 × **0.375** × 1.125 | 同上（浏览器 `Box3` 实测） |
| 茶几 `tableCoffee` | 高 **0.230** | 同上 |
| 门洞（`doorwayOpen` 空框） | 0.486 宽 × **1.010** 高 | 同上 |

### 1.2 问题

**若把玩家当真人（眼高 1.60）：**

```
墙 1.290  <  眼高 1.600     ← 视线越过每一道内墙 0.31 个单位
```

六个房间**当场塌成一个大开间**。找红包变成"站在客厅一眼扫完全屋"，捉迷藏变成"谁也躲不住"。**两个玩法同时死亡，而且死得很安静** —— 画面还是很好看，只是没有玩法了。

### 1.3 解法（二选一）

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 缩小玩家**（推荐） | 玩家身高 **0.42**，眼高 **0.36** → 墙是 **3.07 倍**身高 | **零美术成本**。但必须同步放大移动速度（见下） |
| B. 放大公寓 | 整体 ×2.5 | ❌ 套件**根本没有天花板模型**（`kit_index.json` 里无 ceiling 类构件，只有 `lampSquareCeiling` 这个吊灯）；且剖视娃娃屋的美术语言被毁 |

**选 A。** 顺带得到一个免费的世界观：你是这座公寓里的**玩具 / 小动物**。

### 1.4 缩小玩家之后的三个数字（必须一起改，否则手感会崩）

| 量 | 建议值 | 依据 |
|---|---|---|
| 身高 / 眼高 | 0.42 / 0.36 | 墙 = 3.07 H，沙发靠背 ≈ 1 H，床面 0.375 ≈ 0.89 H，茶几 0.23 ≈ 0.55 H ⇒ **家具是地形** |
| 跑速 | **1.5 单位/s** | ⚠️ **速度要按身高等比放大，但不能按人类的「跑步」比值。** 最初取 5 身高/秒 = 2.1 单位/s，横穿公寓只要 **6.1 秒**——实测下来玩家原话是「**人物速度太快，一下子就逛玩了**」。改成 **3.6 身高/秒 = 1.5 单位/s**：仍远快于真人的慢走（≈ 1 身高/秒），但横穿要 8.5 秒。**改速度 = 所有胜率作废**，已整表重测（`game/VERDICT.md` §6.2） |
| 跳跃高度 | **0.441**（≈ 1.05 H，`v²/2g` at 2.30 / 6.00） | 落地时**可以站到 0.70 m 以下的可站面上**（一次跳 + 一级 0.26 的蹬踏）：茶几 0.23、浴缸 0.42、沙发 0.46、书桌 0.38、厨房台面 0.45 都上得去，0.92 的冰箱上不去。**跳跃高度直接就是关卡设计语言**——本轮「跳上家具」正是这条从手感变成机制的：它现在是关卡的第一承重件（0.83/6 → 5.67/6，VERDICT §6.2） |

---

## 2. 方案一 ·「伪物」捉迷藏

### 2.1 一句话与核心循环

**你是家具。你必须看起来像是"本来就该在这里"的东西。混不进去，就得自己搬过去。**

```
抽卡(6选1) → 落位(挑房间/挑朝向/挑姿态) → 静止期(读搜捕者路径)
      ↑                                              ↓
   结算/换边  ←  被抓 或 熬到终局  ←  风险决策(挪? 跳? 赌他点不点我?)
```

### 2.2 三条承重规则

这三条是所有乐趣的来源，砍掉任何一条，玩法就退化成"谁眼睛尖"。

#### 规则 1 —— **归属律**（用户的原创规则，我把它数据化了）

每件道具有一个**合法房间集合**。`scripts/derive_gamedata.py` 直接从 `js/layout.js` 推出来，**零手工维护**：

| 房间 | 实例数 | 去重后 | **专属件** | **通用件** |
|---|---|---|---|---|
| 卧室 | 16 | 14 | 5 | 9 |
| 卫生间 | 7 | 7 | 6 | 1 |
| 厨房 | 27 | 20 | 18 | 2 |
| 客厅 | 37 | 31 | 18 | 13 |
| 餐厅 | 13 | 11 | 6 | 5 |
| 书房 | 19 | 17 | 8 | 9 |
| **合计** | **119** | **75** | **61** | **14** |

**14 件"通用道具"是全案最漂亮的一块料：**

```
trashcan          ×6   卫生间 + 卧室 + 餐厅 + 厨房 + 客厅 + 书房   ← 六房全通
books             ×7   卧室 + 餐厅 + 客厅 + 书房
sideTableDrawers  ×4   卧室 + 餐厅 + 客厅 + 书房
bookcaseClosedWide×4   卧室 + 客厅 + 书房
bear              ×3   卧室 + 客厅 + 书房
lampSquareTable   ×3   客厅 + 书房
pottedPlant       ×3   餐厅 + 客厅 + 书房
coatRackStanding  ×2   / pillow ×2 / plantSmall1 ×2
plantSmall2       ×2   / rugRectangle ×2 / sideTable ×2 / speakerSmall ×2
```

⇒ 规则直接长出来：**选通用道具 = 拿到全公寓通行证（零归属风险）；选专属道具 = 押注一个房间（高回报）。**

#### 规则 2 —— **违和感（tell）**

当你**同时满足**三个条件 —— ① 处于非法房间 ② 落在搜捕者视锥内 ③ 掷骰通过 —— 你会泄露一个**极轻的信号**：

| 实现 | 表现 | 成本 |
|---|---|---|
| **轮廓呼吸**（推荐） | 整体缩放 **±1.5%**，**固定 0.60 s 周期** | 改一个 `scale` |
| **影子错位** | 地面 blob shadow 与本体错开 3 cm | 加一个 decal |
| **微音**（推荐，与呼吸叠加） | WebAudio 合成 180 Hz 极轻脉冲，3.5 单位内可闻 | 零素材 |

⚠️ **必须是固定周期，不能随机。** 随机 = 玩家学不会 = "我不知道他凭什么发现我" = 差评。

#### 规则 3 —— **检举预算**（整个玩法的承重墙）

搜捕者一局只能"点" **N** 次（难度旋钮 5 / 3 / 1）。点错：扣 1 次预算 + 放出一声**空响**（躲藏者听见就知道"他浪费了一次"，于是敢赌）。

> **没有预算，搜捕者把客厅 31 件去重道具全点一遍就赢了 —— 推理层直接塌掉。**
> 预算不是难度旋钮，预算**就是玩法本身**。

#### 规则 4（我建议加，成本极低，收益极高）—— **擦痕**

道具移动 / 跳跃时，地板留下 **4 秒内衰减**的极短擦痕；站着不动则无痕。

- 把"能跳能跑"从**作弊**变成**有代价**
- 给搜捕者一条可读的线索链（跟着擦痕走）
- 实现：一个平面 + 时间 alpha，或 `DecalGeometry`

### 2.3 「6 选 1」的抽取结构 —— 把运气变成决策

用户定的"6 选 1"我按**来源分层**来配，让每一张卡都是一个赌注：

| 档位 | 抽取来源 | 张数 | 归属风险 | 分值 |
|---|---|---|---|---|
| **本分** | 本房专属池 | 3 | 无（就地合法） | 1× |
| **通客** | 14 件通用池 | 1 | 无（全房合法）但最易被误检 | 1.5× |
| **邻居** | 相邻房间专属池 | 1 | 需搬 1 个房间 | 2.5× |
| **远客** | 任意房间专属池 | 1 | 需横穿公寓 | **4×** |

**这才是"动力"**：分数是**风险的函数**，不是运气的函数。贪一次 4× 远客，就要承担半路撞上搜捕者的概率。

### 2.4 可玩点 / 为什么会再来一局

1. **表演的快感** —— 你不是"藏起来"，你是"演成一把椅子"。而 tell 悬在头上 ⇒ **静止本身是有压力的**，这在游戏里很罕见。
2. **双向猜心** —— 搜捕者用归属律做排除法；躲藏者用静止/擦痕做信息管理。不是单方面躲。
3. **赌注结构** —— 4× 的远客卡天然生成"下一局我要贪一次"。
4. **换边** —— 一局一换边，两种完全不同的紧张感：搜寻方的**枯燥焦虑** vs 躲藏方的**窒息**。
5. **复盘钩子（最便宜、最高价值的留存设计）** —— 结算时给出"你被识别的那一刻"：哪一秒 tell 亮了、搜捕者站在哪个方位、他本来在瞄谁。实现上只是记录 3 个时间戳。

### 2.5 难度设计

**主旋钮不是时间，是检举预算。**

| 旋钮 | 档位 | 为什么有效（数据支撑） |
|---|---|---|
| **检举预算 N** ★ | 5 / 3 / 1 | 直接决定"暴力扫描"能不能赢。**主难度旋钮** |
| 房间数 | 6 / 9 / 12 | 每房专属池独立 ⇒ 候选空间 ∝ 房间数；且"远客"的行程线性变长 |
| 通用件占比 | 高 / 中 / 低 | 通用件多 ⇒ 谁都合法 ⇒ 难找；少 ⇒ 人人违和 ⇒ 好找 |
| 归属严格度 | 相邻房也算合法 / 只认本房 | 直接改变违和事件的发生率 |
| tell 概率 | 100% / 60% / 35% | 控制信息量。35% 时搜捕者开始**怀疑自己的眼睛** |
| 躲藏者人数 | 1 / 3 / 5 | 人多 ⇒ 违和事件增多，但每个更隐蔽（注意力被摊薄） |
| 擦痕衰减 | 5 s / 4 s / 3 s | 越长 ⇒ 移动越值钱 ⇒ 越静态 ⇒ **越闷**。默认 4 s |
| 局时 | 4:00 / 3:00 / 2:30 | **只影响终局压力，不影响推理深度** —— 别指望靠时间调难度 |

#### ⚠️ 一条反直觉结论：难度必须「按房间配平」，不能全局统一

| 房间 | 去重件 | 通用件 | 通用占比 | 真实难度画像 |
|---|---|---|---|---|
| 卫生间 | 7 | 1 | 14% | **看似最难，其实最容易**：池子小、6/7 都是专属 ⇒ 躲藏者几乎必被 tell 出卖；但搜捕者只需检查 7 件 |
| 餐厅 | 11 | 5 | 45% | 均衡 |
| 卧室 | 14 | 9 | 64% | 池小 + 通用多 ⇒ **最好混** |
| 书房 | 17 | 9 | 53% | 均衡偏难 |
| 厨房 | 20 | 2 | 10% | 池大 + 高度专属 ⇒ **违和事件最多**，最"热闹" |
| 客厅 | 31 | 13 | 42% | **最好混但最贵搜** ⇒ 天然的"远客"目标房（4× 分） |

⇒ 设计动作：**把每房的 `tell 概率` 与 `池容量` 独立配平**，而不是给全局一个值。这张表就是配平表。

### 2.6 玩家测评

#### (a) 我的预测（基于同类标杆推理，**不是实测**）

| | 内容 |
|---|---|
| ✅ 会夸 | 演家具的新鲜感；归属律"讲道理"；被漏过那一刻的爽；换边后视角反转 |
| ⚠️ 会骂 | ① **"我不知道他凭什么发现我"** → 必须固定 tell 节奏（0.60 s），不可随机<br>② **"静止一分钟很闷"** → 必须加"搜捕者脚步逼近"的音频紧张感（WebAudio 合成，零素材）<br>③ **"通用件太无脑"** → 通用件的 tell 概率应当更高（自洽：垃圾桶在客厅不违和，但"垃圾桶长腿跑了"很违和） |
| ❌ 会劝退 | tell 纯随机 + 无检举预算 ⇒ 变成纯运气游戏 |

#### (b) 实测方案（能跑的，不是纸上谈兵）

本工作区已有一整套 CDP 无头验收框架，**可以直接跑 AI 自动对局做平衡性回归**：

| 阶段 | 做法 | 产出 |
|---|---|---|
| **1. 机器对局** | `scripts/playtest.js`：1 AI 搜捕 vs 3 AI 躲藏，**N = 200 局**，纯逻辑无渲染（几分钟跑完） | 各旋钮下的胜率曲线 |
| **2. 真人小样本** | 6 人 × 3 局，**热座换边**（同一套玩法两种角色） | 下面的 5 项指标 + 问卷 |
| **3. 回归** | 每次改旋钮重跑阶段 1，胜率偏移 > 8% 即报警 | 防"改平衡改崩" |

**五项可数指标（全部来自自动对局，不靠感觉）：**

| 指标 | 定义 | 目标区间 | 越界说明 |
|---|---|---|---|
| 首杀时长 TTF | 第一次成功检举 / 局时 | **30% – 50%** | < 25% 太空旷；> 65% 根本找不到 |
| 无效检举率 | 点错 / 总检举 | **20% – 40%** | < 15% 太简单；> 50% 规则不透明 |
| 远客卡使用率 | 选 4× 卡的比例 | **15% – 35%** | > 50% 玩家只敢选本分或只敢贪 |
| tell 信噪比 | 真躲藏者 tell / 全体 tell | **0.15 – 0.30** | 偏离即说明 tell 没有信息量 |
| 静态率 | 局时中真正静止的占比 | **55% – 75%** | 太高闷；太低变成追人游戏 |
| 躲藏方胜率 | — | **45% – 55%** | 偏差即需调预算 N |

**真人问卷（3 题，别多）：**
1. 「你被发现的时刻，你觉得公平吗？」1–5 分 —— 目标均值 **≥ 3.8**，这是 tell 调校的唯一验收线
2. 「你想再来一局吗？」是 / 否 —— 目标 **≥ 70% 是**
3. 「哪个房间最好玩？」自由填 —— **这是关卡配平的输入**，答案应当指向「客厅」这类大池房

### 2.7 可行性

| 需要什么 | 现成？ | 说明 | 成本 |
|---|---|---|---|
| 家具模型（会走的"道具"） | ✅ | 83 个已装载模型（75 家具 + 4 墙种 + 3 门种 + 地板） | 0 |
| **碰撞体** | ⚠️ | **无代码，但数据全有**：`data/kit_three.json` 有 140 个模型的实测 bbox，`layout.js` 有每件的 x/z/r ⇒ 碰撞盒**自动生成**，零手工标注 | 低 |
| **导航网格** | ✅✅ | **惊人地免费**：地板就是 **10×8 块 1 m 方砖** ⇒ 直接就是 80 格导航网格 | 0 |
| 角色控制器 / 跳跃 / 重力 | ❌ | `PointerLockControls`（three 自带）+ capsule + 简单重力，约 200 行 | 中低 |
| **角色动画** | ❌ | **而且不需要** —— 躲藏者就是道具本身；搜捕者可以是个飘浮的探照灯。**这绕开了套件唯一真正缺的东西** | **0** |
| 变形（6 选 1） | ✅ | 就是换 `kit.instance(name)` | 低 |
| AI（躲藏选点 / 搜捕搜索） | ❌ | 可**完全避开寻路**：躲藏者按"合法池 + 门口视线"打分选点；搜捕者按"巡视 → 扫视 → 按预算检举" | 中 |
| 音频 | ❌ | WebAudio `OscillatorNode` 合成（脚步 / 叮 / 空响 / tell 脉冲），**零素材** | 低 |
| 光影 | ✅ | **建议不改** —— 保留 Kenney 的明快，用**视锥 + tell** 制造紧张，**不要做暗场景**（会毁掉这套 CC0 素材的美术语言） | 0 |

#### 门：两条免费的关卡设计（实测）

| 门 | 模型 | 实测证据 | 游戏含义 |
|---|---|---|---|
| 卧室 | `doorwayOpen` | 28 tri / 单 mesh / 仅 wood / 无 Z 向伸出 | **敞口** —— 走廊能直接望进去 |
| 厨房 | `doorwayOpen` | 同上 | **敞口** |
| 卫生间 | `doorway` | **188 tri**（7×）+ **metal**（把手）+ Z 伸出至 **+0.057** | **关着** ⇒ 天然的「最后搜索房」/「上锁房」 |
| 入户 | `doorwayFront` | 156 tri + **glass**（观察窗）+ Z 伸出至 +0.057 | **关着且有玻璃** ⇒ 天然的关卡边界 / 逃脱目标 |

> 「卫生间上锁」这个点子不是我想出来的，是从 GLB 的三角面账目里读出来的。

### 2.8 风险（诚实说）

1. 🔴 **tell 的透明度是全案单点**。太明显 = 一眼看穿；太隐晦 = 玩家觉得被随机。**必须先用 AI 对局把"tell 信噪比"压进 0.15–0.30，再交给真人。**
2. **没有天花板** ⇒ 玩家跳到高处会飞出建筑。要么在 y = 1.25 封一个不可见顶，要么禁止跳到家具顶面以上。
3. **AI 躲藏者的"聪明"很难做得不假**：如果它总是选到最优位，玩家会觉得被碾压；建议给它**可控的失误率**（如 20% 选次优位），并把失误率做成难度档。

---

## 3. 方案二 ·「寻红」找红包

### 3.1 一句话与核心循环

**你以小人视角走进公寓，限时找齐所有红包。**

```
推进 → 扫视(视锥内的红色块) → 判定(红包红 还是 沙发红?) 
  → 换角度/低头/绕背确认 → 拾取 → 下一间
```

### 3.2 可玩点

1. **视觉顿悟（aha）** —— 找物类的快感来源是"我居然之前从它旁边走过"。这必须靠**遮挡层次**制造，**不是靠把红包缩小**。
2. **扫视路径优化** —— 高手的乐趣是"我怎么用 20 秒扫完一间"。这是可提升、可炫耀的技能。
3. **近色博弈（本方案最硬的料）** —— 套件里**已经有 24 个模型带红色槽位**：

   ```
   bedBunk, bedDouble, bedSingle, benchCushion, benchCushionLow, books,
   chairCushion, chairDesk, loungeChair, loungeChairRelax, loungeSofa,
   loungeSofaCorner, loungeSofaLong, loungeSofaOttoman, pillow, pillowLong,
   rugRectangle, rugRound, rugRounded, rugSquare, stoolBar, stoolBarSquare,
   tableCloth, tableCrossCloth
   ```

   **红包红选 `#C8102E`**，与套件那抹红 `#F05E57` 的 CIE76 色差 **ΔE = 19.6**：

   | 候选 | 色差 vs `#F05E57` | 定位 |
   |---|---|---|
   | **`#C8102E` 深正红（中国红）** | **19.6** | ✅ **推荐** —— 恰在"远距扫视勉强可辨"阈值上：一眼是同一族红，**必须靠近或换角度才能确认** |
   | `#E4002B` 亮正红 | 24.4 | 偏易（扫视即可分辨） |
   | `#D7261E` 朱红 | 22.4 | 偏易 |
   | `#8E0F22` 暗红 | 30.8 | 用作红包**阴影面**（与 `#9B4C49` 只差 23.0，暗处更难辨） |

   参考：换色阈值 ≈ 2.3 / 侧眼可辨 ≈ 5 / 一眼可分 ≈ 10 / 远距扫视可辨 ≈ 20。

   ⇒ **难度来源是"同族红的分辨"，而不是"把红包缩到 3 像素"。** 这是这个方案能不能立住的关键判断。
   > **⚠️ 2026-09-25 更正：上面这句「关键判断」已被实测否掉。**
   > 完整数据在 `game/VERDICT.md`。要点：红色家具是**路标，不是障碍**。
   > 把「红装饰会不会骗你」这**一个开关**翻掉，玩家人格在 180 s 的胜率
   > 从 **91.7% 掉到 62.5%** —— 去掉同族红，玩家**更差**，因为他失去了
   > 一套把那 33 m 走遍的巡回启发式（随机游荡从 80.7 s 涨到 115.5 s）。
   > 而且这个读数**符号不稳**：60 s 预算时是 −4.2 pt，换一条随机流还能再飘 33 pt。
   > ⇒ 颜色可以留作氛围，**不能承重**。真正的难度来源是守卫（§3.8）。


4. **半透明与内部** —— **18 个模型带玻璃槽位**（`kitchenFridge` 门、`kitchenMicrowave` 门、`tableCoffeeGlass`、`bathroomMirror`、`shower`、`washer`…）⇒ 红包可以放在玻璃后面，靠**材质**而非距离制造难辨。
5. **发光物** —— 7 个 `lamp*` 模型带自发光 ⇒ 可做"红包的微光呼吸提示"（最后 1 个红包的防挫败机制）。

### 3.3 动力（找物类最怕"没有理由再来一次"）

| 机制 | 作用 |
|---|---|
| **每日种子** | 同一布局 + 固定随机种子 ⇒ 可比较、可分享、可竞速。**这是找物类留存的命脉** |
| 星级（时间 + 完美收集） | 单局的自我挑战 |
| 连击 + 剩余时间浮动显示 | 把搜索从"慢工出细活"推向"贪速度" |
| 排行榜（localStorage 即够） | 人际比较 |
| **「告密」** | 卡住时花 30 秒换一个"大致方向"（不是精确位置）。**防挫败，成本极低** |

### 3.4 难度设计

| 旋钮 | 档位 | 说明 |
|---|---|---|
| 房间数 | 6 / 9 / 12 / 16 | 用户已提出（扩房成本见 §5） |
| 红包数 | 3 / 6 / 10 / 16 | **与房间数解耦**：房多红包少 = 空地多 = 更累；房少红包多 = 密度高压 |
| **可见度分级** ★ | 明放 / 半遮 / 全遮 / 内部 | **这才是真难度旋钮**。建议配比 **20 / 35 / 30 / 15 %** |
| 干扰红密度 | 低 / 中 / 高 | 直接用现成的 24 个红模型：把红地毯、红凳、红枕头摆在红包附近 |
| 时间 | 90 / 60 / 45 s | **只在最后阶段收紧** |
| 是否需交互 | 纯观察 / 需搬开枕头·开门 | ❌ **需交互 = 成本陡增**（要做交互与物理），**建议第二版再做** |

### 3.5 ⚠️ 一个我必须明确反对的设计

> **只做"找"的找红包，是一道单人视觉搜索题 —— 没有对手、没有失败戏剧、没有故事。它的天花板是《Hidden Folks》，不是"游戏"。**

所以升级路径必须**引入对抗**，两个方向（可叠加）：

| 方向 | 做法 | 复用度 |
|---|---|---|
| **(a) 守卫** | 公寓里一只按路线巡逻的宠物（AI），被它的视锥扫到就扣时间 / 被赶出该房间 ⇒ **纯搜索立刻变潜行** | ⭐ **与方案一的 AI 搜捕者高度重合**（视锥检测 + 巡逻 + 预算）—— 同一套技术底座 |
| **(b) 时限抢夺** | 两个 AI 竞争者（或热座第二人）同时找，谁先拿谁得 ⇒ 变成"谁扫视路径更优" | 需要第二套输入 / AI 竞争逻辑 |

### 3.6 玩家测评

#### (a) 我的预测

| | 内容 |
|---|---|
| ✅ 会夸 | 明快好看、上手零门槛、"我居然从它旁边走过"的顿悟、每日种子 |
| ⚠️ 会骂 | ① **"太累了"**（长扫视导致视觉疲劳）→ 需要"已扫完的房间在地图上打勾"，降低认知负荷<br>② **"3 分钟找不到最后 1 个很烦躁"** → 告密 + 最后 1 个微光呼吸<br>③ **"为什么这个红是沙发红而不是红包"** → 这其实**是好事**，但必须在教学关明确一次"红包是这个红" |
| ❌ 会劝退 | 无对手 + 无评分 + 无种子 ⇒ 玩完一次就走 |

#### (b) 实测指标

| 指标 | 目标区间 | 越界说明 |
|---|---|---|
| 平均单红包搜索时长 | 25 – 40 s（6 个 / 3 分钟） | 太短 = 干扰红没起作用。**本工具链答不了这一项**（AI 走完美路径，绝对值不可引用）；可引用的是相对量：完美路由 **23.4 s** vs 按玩家打法找全 **160–188 s**（VERDICT §3 / §6.2） |
| **假阳性确认率**（走过去看是干扰红） | **30% – 50%** | < 25% 干扰物没用；> 60% 太像、会烦。**实测 45.8%** ✅ 达标（但这 45.8% 是被骗的**次数占比**，不是代价占比） |
| 房间扫视覆盖率（终局） | ≥ 90% | 低于则说明有房间被玩家放弃 |
| 失败分布 | 应集中在"最后 1–2 个" | 若均匀分布 ⇒ 说明难度是随机的，不是设计的 |
| 首次通关率 | 30% – 50% | 太高无挑战；太低无成就感 |
| 同种子重玩率 | ≥ 1.5 次 | 找物类的留存体检 |

### 3.7 可行性

| 需要什么 | 现成？ | 说明 | 成本 |
|---|---|---|---|
| 红包模型 | ❌ | 套件**没有**。但极便宜：折叠信封 = 2 面 + 1 翻盖，约 **12–20 tri**。可用已有的 `blender-headless-game-asset-qa` 技能生成，或直接在 three.js 里用 `BoxGeometry` 微调顶点 | 低 |
| 红包材质 | ✅ | 一个 `MeshStandardMaterial`：`#C8102E` + 暗面 `#8E0F22`，`roughness 0.55`（纸感，别用光泽） | 0 |
| 第一人称 + 碰撞 + 视锥 | ❌ | **与方案一完全同源的同一套基础设施** | 中低 |
| 关卡扩展 | ✅ | 见 §5 | 低 |
| 计时 / 星级 / 种子 / 排行榜 | ❌ | 纯逻辑，与 3D 无关 | 低 |

### 3.8 验证结论（2026-09-25 无头实测）

> 工具链：`game/core/` 纯逻辑核心（零 three.js / DOM / 依赖）+ `game/arena/fromLayout.js` 适配器
> + `game/arenas/room_scene.json` 快照。**换一栋楼 = 换一个快照，验证脚本原样可跑。**
> 复现：`node scripts/verify_game.mjs 24`（95 s）/ `diag_oracle.mjs` / `diag_explore.mjs`。
> 可玩渲染层（`play.html`）另有 **69 条**无头断言：`node scripts/verify_play.mjs`。
> 完整结论见 **`game/VERDICT.md`**。

| 设计里的说法 | 实测 | 结论 |
|---|---|---|
| 红包都够得着、都看得见 | 23248/23248 格可达；6/6 可达可发现；最远 9.79 m | ✅ 成立 |
| 守卫会巡遍全屋 | 300 s 单人巡逻覆盖 6 房，最长间隔 **59.5 s**（300 s 走 240.6 m） | ✅ 成立 |
| 20/35/30/15 暴露分层 | 231 个藏点：open 186 / partial 44 / **concealed 0** / inside 1 / hidden 67 | ❌ concealed 档恒空 |
| 15% 藏在玻璃后 | 计划里 2 个玻璃体积，只有冰箱可用 | ⚠️ 压在单一锚点 |
| **难度来自同族红的分辨** | 翻掉该开关：180 s 胜率 91.7% → 62.5%（去掉颜色反而更差）——**旧游戏读数**，见 VERDICT §6.2 | ❌ **否** |
| 「越难 = 监控区越大 + 红包越小」 | 扇区 0 → 11.4 → 19.2 → 30.2 m² 单调增，红包 18.4×11.5 → 11.2×7.0 cm 单调减 | ✅ 方向成立，但**不是第一承重件** |
| **「跳上家具」是不是真机制** | 同走法、同预算、同种子：**不许**爬上家具 180 s 得 **0.83/6**，**允许**得 **5.67/6** | ✅ **第一承重件** |
| 24 种子重测四张难度卡（各按自己的预算） | 见习 240 s **66.7 %** · 标准 180 s **33.3 %** · 紧张 180 s **29.2 %** · 硬核 180 s **12.5 %**；两个对照见 §6.2 | ✅ 旋钮可定价 |

**四条意外收获**

1. **路由是免费的。** 一个开局就被告知全部红包位置、不扫视的走法，
   拿齐 6 个只要 **23.4 s / 29 m / 0 次扫视**，而且在 60 s 预算下都 100% 成功。
   （换算：`29 ÷ 1.5 + 6 × 0.5 ≈ 22.3 s`，多出来的 1.1 s 是**爬上去的代价**。）
   ⇒ **所有难度都在「找」**，优化布局/路程没有回报，优化「藏」与登高才有。
2. **承重顺序（`game/VERDICT.md` §6.2 实测）：跳上家具 > 守卫 > 红包尺寸 > 预算。**
   - **跳上家具 · 第一**：同走法、同预算、同种子，**不许**爬上家具时 180 s
     只找得到 **0.83/6**，**允许**是 **5.67/6**。红包既然不放地上，而 0.36 m
     的眼高看不见 0.45 m 台面上有什么，「往上爬」就是搜索的后半段。
   - **守卫 · 第二**：玩家型人格 180 s 胜率 **66.7% → 33.3%（−33.4 pt）**，
     代价 2.08 次被抓，而罚时是玩家能靠走位减少的；完美走法基本不受影响
     ⇒ **守卫惩罚磨蹭，不惩罚熟练**，是好设计。
   - **预算 · 不是难度旋钮，是闸门**：120 s 下四个档位**全部 0.0 %**
     （收集量 2.13–3.67/6），因为一局的时间现在主要花在「找」上，
     调小预算会把所有档位一起压死。所以 `紧张` / `硬核` 的时限从 120 s 提到 180 s。
     建议难度带：**玩家型人格 + 守卫、180 s ⇒ 胜率 12.5%–33.3%**。

   > 这一段推翻了旧结论「守卫是唯一能一次砍掉近半个胜率的旋钮」——
   > 那是在红包还摊在地上、站在门口一眼就看见的年代量的。
   > 详细对照、以及「四档全部 0.0 %」那次量具事故，见 `game/VERDICT.md` §6.2。

   > 这一组数字换过一次。旧表（−50.0 pt / 41.7 %）来自一个**会穿墙**的守卫：
   > 它沿直线穿过墙和沙发，所以「抓到你」和「走到你身边」是两回事。
   > 补上碰撞之后它一度卡死（600 s 只走 88–137 m），修好巡逻规划才得到上面这组。
   > 修法与实测见 `game/VERDICT.md` §8 第 5–6 条、§2.4。
3. **分层配额必须让位给覆盖优先。** 分层优先会让最小的房间（卫生间）
   **每个种子都掉红包**（6.00/6 → 5.38/6）。改成「六房都有 + 暴露度下界」，
   并把 `concealed` 档从设计里删掉或注明「本户型不适用」。
4. **难度可以拆成两个互不冲突的旋钮，但第二个旋钮会偷偷改玩法。**
   把红包按难度缩小（18.4 → 11.2 cm）看上去只是美术调整，实际上
   `noticeRange()` 是由红包**面积**算出来的，红包小一圈 = 玩家与 AI 的察觉距离
   一起短一截。这不是缺陷，是免费的第二难度轴；代价是**任何引用旧胜率的说法都作废**，
   必须整表重测。同时把监控区做成真的光（而非贴花），玩家才可能一眼读出
   「它盯着的这块地有多大」——难度旋钮要能被看见，才叫旋钮。

**若要让「近色」真的承重，需要改计划（三选一）**

- 干扰红**集中到死角 / 远离红包的单独房间**，使「追错」= 一趟纯浪费；
- 让红包成为**视野里最显著**的红（大面积红包装 / 被红布半掩），
  使「最大的就是真的」这条启发式**系统性骗人**；
- 或把确认成本从 0.5 s 提到 1.5–2 s（现在只占总时钟 2.2%，太便宜）。


---

## 4. 对比与推荐

### 4.1 我推荐先做方案一，三条理由

1. **它有对手。** 方案二是题，方案一是局。对抗性是这两者之间唯一重要的差别。
2. **它的技术底座完全覆盖方案二。** 碰撞 / 导航 / 视锥 / AI / 音频做完，方案二变成"去掉对手 + 加个计时器"的**减法**。
3. **它与现有资产咬合最紧。** 躲藏者就是道具本身 ⇒ **不需要角色动画**，而角色动画正是这套 CC0 素材唯一真正缺的东西。选方案一 = 把唯一的短板绕过去了。

### 4.2 但不要放弃方案二

把它做成方案一里的**教学关 / 单人模式**，共用同一套基础设施，**边际成本极低**。
理由很实在：方案一的教学成本高（归属律 + tell + 预算三件事要讲），而"进屋找红包"是零解释成本的。**先用方案二教会玩家"这个公寓长什么样、这门能过那门不能过"，再放他进方案一的对抗局。**

---

## 5. 房间扩展成本（用户两次强调，所以给实测清单）

### 5.1 结论：扩房是**改数据**，不是写代码

| 步骤 | 文件 | 改什么 | 自动获得什么 |
|---|---|---|---|
| 1 | `js/layout.js` | `PLAN = {w, d}` 改大 | `buildFloor` 自动铺满（80 → N 块）；`requiredModels()` 自动纳入新模型 |
| 2 | `js/layout.js` | `WALLS` 增删条 `{axis, at, from, to, side, kinds}` | 墙 / 窗 / 门洞自动生成 |
| 3 | `js/layout.js` | `ROOMS` 加一条房间 + `items[]` | 摆放、合并、拾取高亮、房间计数**全自动** |
| 4 | `js/layout.js` | `ZONES` 加一条（`eye` + `face` + `hue`） | 标签 + 预设机位自动出现 |
| 5 | `js/layout.js` | `DOORS` 加/改门 | — |
| 6 | 验收 | 跑 `scripts/shoot.js` | 静态锚点 + 运行时计数 + **机位朝向断言自动覆盖新房间**（`ZONES` 是注册式驱动） |

### 5.2 唯一需要新写的三样（一次写完，之后每加一房都是零成本）

| 组件 | 内容 | 代码量（估） |
|---|---|---|
| `deriveOwnership(ROOMS)` | 归属矩阵 + 通用/专属分类（**已验证可从 `layout.js` 自动推出 61 专属 / 14 通用**） | ~20 行 |
| 碰撞盒生成 | 从 `data/kit_three.json` 的 bbox + `layout` 的 x/z/r 生成 OBB→AABB（注意少数非 90° 旋转：12°、150°、168°、195°、200°、210°、30°、45°、75°） | ~40 行 |
| 导航网格 | 地板瓷砖即网格 | **0 行** |

### 5.3 性能预算

| | 现状（6 房） | 扩到 12 房（估） |
|---|---|---|
| 三角面 | 22,699 | ~45,000 |
| 材质 | 18 | 18（调色板固定 15 色 + 派生槽） |
| draw call | 81（≈13/房，`mergePlaced` 已按材质合并） | ~160 |
| 结论 | 1660 SUPER 完全无压力 | 仍然无压力；瓶颈只可能在 draw call，而合并策略已就位 |

---

## 6. 实施路线（每阶段都有可数验收，沿用本工作区的验证纪律）

| 阶段 | 产出 | 验收断言（可数事实，不看截图） |
|---|---|---|
| **G0** | `js/collide.js` + `js/player.js` | 碰撞盒数量 **==** 布局物件数；从 6 个房间中心各发 1 条向门口的水平射线，必须被墙挡住 |
| **G1** | `js/game/ownership.js`（归属矩阵 + 6 选 1） | 61 专属 / 14 通用；每房池 ≥ 7；每张抽卡必属于某个合法池 |
| **G2** | `js/game/hide.js`（回合循环 + 预算 + tell） | tell **只在**「非法房间 + 视锥内」触发；预算耗尽后拒绝检举 |
| **G3** | `js/ai/`（AI 搜捕 + AI 躲藏） | `scripts/playtest.js` 跑 200 局，输出胜率 + 5 项指标表 |
| **G4** | HUD / 结算 / 复盘时间轴 | 结算数据含"被识别时刻"的 3 个时间戳 |
| **G5** | 房间 6 → 9 → 12（**只改数据**） | 跑 `shoot.js`，新房间的机位朝向断言自动通过 |
| **G6** | WebAudio 音效（零素材） | `AudioContext` 只有 1 个；无 404 |
| **G7** | 方案二（教学关）：红包模型 + 计时 + 种子 | 红包三角面在 12–20 之间；同一种子两次运行布局逐字节一致 |

---

## 7. 三个待你拍板的决策

| # | 问题 | 选项 | 我的倾向 |
|---|---|---|---|
| 1 | **玩家人称** | 第一人称（沉浸、紧张） / 第三人称俯视（看得清、好上手） | 方案一用**第一人称**（紧张感来源）；但保留现有的 8 个俯视机位做**地图模式**（按 Tab 切换），教学关用俯视 |
| 2 | **单机还是联机** | 单机 AI / 局域网 WebSocket 房间 | **v1 只做单机 AI**。联机的同步与作弊问题会吃掉整个工期，而 AI 对局还能顺手做平衡性回归 |
| 3 | **先做哪个方案** | 方案一 / 方案二 / 两个都做（方案二当教学关） | **先方案一，方案二作为教学关一起做**（共用底座，边际成本最低） |

---

## 附录 A · 一键复现所有数字

```bash
python scripts/derive_gamedata.py    # 归属矩阵 + 颜色普查 + 色差 → work/gamedata.json
python scripts/probe_doors.py        # 门扇判定（2026-09-25 修好，见附录 B）
python scripts/shoot.js              # 场景验收（现有）
```

## 附录 B · 已修：`probe_doors.py` 的两个 bug

> 本节原记为「本轮遗留」。2026-09-25 工程化时修掉了，记下的**真实根因** ——
> 原来的描述只说对了症状。

- **症状**：`census()` 抛 `IndexError: list index out of range`（`pos[idx[t + 2]]`）。
- **根因 1（真正的病）**：源码写的是 `if 'index' in pr:` —— glTF 的键是 **`indices`**（复数）。
  这个笔误让分支**恒为假**，于是每次都退到 `idx = list(range(len(pos)))`，把 *顶点表*
  当成 *三角形列表* 来扫。**它不报错，它只是换一套错的数据继续算** —— 就算不崩，
  那份面片账目也不是真的。
- **根因 2（崩的那一步）**：`range(0, len(idx), 3)` 在 `len(pos)` 不是 3 的倍数时越界读到
  `idx[t + 2]`。兄弟脚本 `probe_faces.py` 用的是 `range(0, len(idx) - 2, 3)`，本脚本没跟上。
- **修法**：改键名 + 改步进上界，各一行。
- 门扇的结论**始终由 `data/kit_index.json` 的三角面/材质账目独立确证**（188 tri vs 28 tri、
  metal 把手、Z 向 +0.057），不依赖本脚本；修好它只是把一件「面片法线普查」工具还回工具架。
- **修复后它独立复现了同一条结论**：`doorway` 列出 4 块 `±Z` 面板、每块 **0.0519 m²**
  （在 0.02 m² 阈值之上），`doorwayOpen` 则是「no panel large enough to be a door leaf」。
  工具回到架上，而且立刻投了赞成票 —— 这也是「先修量具，再下结论」的又一次兑现。

> 教训：恒为假的条件分支是一条**静默降级**路径。修的时候要连「这个分支到底走没走到」
> 一起验，否则会把「量具坏了」读成「被测对象坏了」。
